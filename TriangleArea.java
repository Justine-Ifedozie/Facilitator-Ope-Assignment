import java.util.Scanner;
public class TriangleArea {
	public static void main (String[] args) {
	Scanner keyboardInput = new Scanner(System.in);

	System.out.print("Enter two points first: ");
	double side1a = keyboardInput.nextDouble();
	double side1b = keyboardInput.nextDouble();

	System.out.print("Enter two points second: ");
	double side2a = keyboardInput.nextDouble();
	double side2b = keyboardInput.nextDouble();

	System.out.print("Enter two points third: ");
	double side3a = keyboardInput.nextDouble();
	double side3b = keyboardInput.nextDouble();

	double s = 



	double pointsXySquareRoot = Math.sqrt(pointsXy);

	System.out.println(pointsXySquareRoot);

	System.out.println(Math.sqrt(pointsXy));

	}
}